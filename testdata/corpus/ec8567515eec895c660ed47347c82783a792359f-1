//
{namespace y}{template.helloWorld}{msg desc=""}!{/msg}{/template}/**@param name */{template.helloName}{if a()and$name}{msg desc=""} {$name}!{/msg}{else}{call.helloWorld/}{/if}{/template}/**@param names st of names of the people to say hello to.
 */
{template .helloNames}
  {foreach $name in $names}
    {call .helloName}
      {param name: $name /}
    {/call}
    ½¿ï½¿ï½¿ï{if not isLast($name)}
      <br>  // break after every line except the last
    {/if}
  {ifempty}
    // If names list is empty, say "Hello world".
    {call .helloWorld /}
  {/foreach}
{/template}